const user = require("./user");
const auth = require("./auth");
const blogPost = require("./blogPost");
const comment = require("./comments");
const favorite = require("./favorite");

module.exports = { auth, blogPost, user, comment, favorite };
